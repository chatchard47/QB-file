CLS
   PRINT "score le Area by thitima"
   READ year
   INPUT "code="; code
   INPUT "name="; name$
   INPUT "midterm score"; midterm
   INPUT "final score"; final
   INPUT "affective score"; affectve
   total = (midterm + final) + affective
   PRINT "code"; code
   PRINT "name"; name$
   PRINT "year"; year
   PRINT "total"; total
   DATA 1
END

